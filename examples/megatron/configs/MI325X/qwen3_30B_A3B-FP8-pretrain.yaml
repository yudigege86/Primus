work_group: ${PRIMUS_TEAM:amd}
user_name: ${PRIMUS_USER:root}
exp_name: ${PRIMUS_EXP_NAME:qwen3_30B_A3B-pretrain}
workspace: ${PRIMUS_WORKSPACE:./output}

modules:
  pre_trainer:
    framework: megatron
    config: pre_trainer.yaml

    # model to run
    model: ${PRIMUS_MODEL:qwen3_30B_A3B}.yaml
    overrides:
      # log
      wandb_project: "Primus_Qwen3_30B_A3B_Pretrain"
      stderr_sink_level: DEBUG

      # debug
      moe_router_force_load_balancing: true
      log_avg_skip_iterations: 2
      log_avg_reset_interval: 50

      # hyper parameters
      train_iters: 10
      micro_batch_size: 8
      global_batch_size: 512
      seq_length: ${PRIMUS_SEQ_LENGTH:4096}
      max_position_embeddings: ${PRIMUS_MAX_POSITION_EMBEDDINGS:4096}
      lr: 1.0e-5
      min_lr: 0.0
      lr_warmup_iters: 2
      lr_decay_iters: null
      lr_decay_style: cosine
      weight_decay: 0.1
      adam_beta1: 0.9
      adam_beta2: 0.95
      eod_mask_loss: true
      init_method_std: 0.008
      norm_epsilon: 1.0e-6

      # recompute
      # Recomputing full MoE layers (router + permute + 128-expert grouped GEMM)
      # is very expensive (~2k tokens/s per layer here), so recompute only 4 of
      # 48 layers. FP8 weights leave enough memory headroom to keep the rest of
      # the activations resident.
      recompute_granularity: full # full, selective
      recompute_method: block # uniform, block
      recompute_num_layers: 4 # int

      # parallel
      tensor_model_parallel_size: ${PRIMUS_TP:1}
      pipeline_model_parallel_size: ${PRIMUS_PP:1}
      expert_model_parallel_size: ${PRIMUS_EP:8}
      overlap_grad_reduce: true
      overlap_param_gather: true

      # data
      mock_data: true
      train_data_path: ${PRIMUS_TOKENIZED_DATA_PATH:null}
      valid_data_path: null
      test_data_path: null

      # optimizer
      use_precision_aware_optimizer: true
      main_grads_dtype: bf16
      exp_avg_dtype: bf16
      exp_avg_sq_dtype: bf16

      # rope fusion
      enable_experimental: true
      apply_rope_fusion: true

      # ckpt
      finetune: false
      auto_continue_train: false
      load: null
      no_load_optim: null
      no_load_rng: null
      save: null
      save_interval: 20000
      no_save_optim: null
      no_save_rng: null
      disable_last_saving: true
      ckpt_format: torch
      eval_iters: 0

      # Turbo
      enable_primus_turbo: true
      # NOTE: turbo attention is intentionally OFF for FP8. The BF16 turbo kernel
      # inside the FP8 pipeline is ~8% slower than the default TE attention, and
      # the FP8 flash-attention kernel (enable_turbo_attention_float8) is not
      # compatible with this model's GQA head layout.
      use_turbo_attention: false
      use_turbo_grouped_gemm: false
      use_turbo_rms_norm: false # bug

      # deepep
      use_turbo_deepep: true
      moe_shared_expert_overlap: false
      moe_router_dtype: fp32

      # 64 or 80 for ep8, 32 for ep16-64 is best practice. 80 is required for
      # the fast regime here: dropping below ~78 CUs cliffs throughput ~13%
      # (DeepEP comm stalls), so keep 80.
      turbo_deepep_num_cu: 80
      turbo_deepep_use_comm_stream: false

      # sync-free moe support stage 1-2, 0 means not use sync-free moe
      # stage 2 is recommended for better performance
      turbo_sync_free_moe_stage: 1

      # Cross entropy flags
      cross_entropy_fusion_impl: "te"
      cross_entropy_loss_fusion: true

      # enable fp8 training
      fp8: hybrid
      # FP8 delayed scaling (default) is fastest, but the delayed_fp8_scaling
      # stream patches crash with a GPU memory fault (sum_and_scatter) at ~iter
      # 6; disable them. Alternative: fp8_recipe=tensorwise (no patches).
      fp8_recipe: delayed
      disable_delayed_scaling_patches: true
      moe_use_legacy_grouped_gemm: true
      gradient_accumulation_fusion: true
